# Home — `/`

Draft copy for Phase 4 assembly. Section order follows the alternating dark/light rhythm
in `UI_UX_SPEC.md` §3. Ground is noted per section. Run through `human-voice` before
committing to Astro — tracked in this file's own status line at the bottom.

## Hero — dark

Eyebrow (mono, uppercase): `NEXUS CONSULTING`

H1:
> IT that has to stand up to scrutiny

Subhead:
> Compliance-driven IT for Zambian businesses that answer to a regulator — and the
> businesses that serve them.

Primary CTA: `Start with a scoping call` → `/contact/`
Secondary CTA: `See what we build` → `/work/`

Node-graph SVG device behind the hero text, per `p2-nodegraph`.

## The gap — dark, continues hero register

> Most of our clients answer to someone. A regulator, an auditor, a board, or a customer
> who wants to know where their data is kept. That work has a different shape from
> ordinary IT support: the question is not only whether a control works, but whether you
> can demonstrate that it works, to someone whose job is to doubt you.
>
> That is the gap we are built for. We deploy open-source and commodity technology to the
> standard a supervised institution needs, then document it the way an examiner expects to
> read it. We stay on to run it afterwards.

Pull quote (existing brand line, keep verbatim — it is load-bearing):
> We are small on purpose. You deal with the person who does the work, not an account
> manager who relays it.

## The wedge — light

Eyebrow: `WHAT WE LEAD WITH`

H2:
> Closing the audit finding without a per-seat licence

Body:
> Every regulated lender in Zambia carries some version of the same finding: staff
> security awareness is not being measured, and there is nothing on file to prove
> otherwise. The platforms built to close it are priced per seat in US dollars — for a
> sixty-person microfinance institution that is not a budget line.
>
> We built the affordable version inside a Bank of Zambia-supervised institution, where it
> closed the finding it was commissioned to close. That is what we now deploy and operate
> for clients — monthly phishing simulation, automatic remedial training, new-joiner
> onboarding, quarterly content refresh. No per-seat fee, no forex exposure.

CTA: `See how it works` → `/services/security-awareness/`

## Six things, one team — dark

Eyebrow: `WHAT WE DO`
H2: `Six things, and we do all of them ourselves`

Six-item grid (icons TBD — SVG line marks, not stock icon packs):
1. **Cyber security & compliance** — Security awareness and phishing simulation, gap
   assessments, policy packs, incident-response plans, and the documentation an examiner
   expects.
2. **Microsoft 365 & infrastructure** — Tenant hardening, MFA and Conditional Access,
   Defender, mail flow. Firewalls, site-to-site VPN and 3CX telephony.
3. **Web design & development** — Business websites built to load quickly on a Zambian
   mobile connection, structured to be found in search, and handed over properly.
4. **Hosting & domains** — Managed hosting on infrastructure we operate, domain
   registration, business email, SSL, backups and monitoring — on an annual retainer.
5. **Business systems** — Where an off-the-shelf product doesn't fit, we build. A loan
   management system and a credit-risk dashboard are both running in production today.
6. **ICT supply & procurement** — Hardware, licensing and network equipment sourced and
   configured, so what arrives is ready to use rather than ready to unbox.

Pull quote:
> If we cannot do it properly, we say so and point you to someone who can. That has cost
> us work and kept us clients.

CTA: `All services` → `/services/`

## Selected work — light

Eyebrow: `SELECTED WORK`
H2: `What we have actually built`

Two-card teaser (full detail lives on `/work/`):
- **NdalamaHub** — Lending management system. Borrower intake, application review queue,
  disbursement controls, a rollover engine and a collateral register. Live in production.
- **[Second case study — pending client consent, see `p5-consent`]**

CTA: `See the work` → `/work/`

## Close — dark

H2: `Start with the scoping call.`
Body:
> If you are carrying a finding, renewing a website, or simply unsure whether your current
> setup would survive being looked at closely — that first conversation costs you nothing
> and usually tells you what you need to know.

CTA: `Book a scoping call` → `/contact/`

---

**Voice pass status:** run `human-voice` on the final assembled page copy before commit,
per `CLAUDE.md` global instructions — this draft reuses phrasing already approved in the
company profile, so the pass is confirmatory rather than corrective, but it must still run
and be recorded, not skipped because the source read clean.
